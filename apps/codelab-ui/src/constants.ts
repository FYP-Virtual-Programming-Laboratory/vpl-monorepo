/**
 * Key for accessing the authentication token data in local storage
 */
export const AUTH_TOKEN_KEY = "authToken";

/**
 * Key for accessing the authenticated user data in local storage
 */
export const AUTH_USER_KEY = "authUser";
